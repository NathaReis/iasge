export interface Perfil
{
    type: string,
    eventos: boolean,
    departamentos: boolean,
    associados: boolean,
    all_view?: boolean,
    config: boolean,
    home: boolean,
    escalas: boolean
}